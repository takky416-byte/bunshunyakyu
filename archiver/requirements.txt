requests>=2.31
beautifulsoup4>=4.12
markdownify>=0.11

# --render / --infinite-scroll オプション(JS実行後のHTML取得やスクロール自動操作)を
# 使うときだけ追加で必要:
# pip install playwright
# playwright install chromium

# post_blog.py(ブログ自動投稿)を使う場合は必須:
# pip install playwright
# playwright install chromium
